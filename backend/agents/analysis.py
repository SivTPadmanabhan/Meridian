"""Analysis node.

Phase 2 stub: returns {} so the graph compiles and P0/P1 runs route through here
to END. Phase 3 replaces this with real RAG retrieval + root-cause reasoning.
"""

from backend.agents.state import MeridianState


async def analysis_node(state: MeridianState) -> dict:
    return {}
